[project]
name = "agent-proxy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mitmproxy>=10",
    "flask>=3",
    "pyyaml>=6",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["test_addon.py", "test_functional.py"]
